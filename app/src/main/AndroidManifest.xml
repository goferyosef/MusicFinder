<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <!-- Required on Android 11+ to detect installed apps -->
    <queries>
        <package android:name="com.brave.browser" />
        <package android:name="com.google.android.youtube" />
        <package android:name="com.google.android.apps.youtube.music" />
        <package android:name="com.spotify.music" />
        <package android:name="deezer.android.app" />
        <package android:name="com.aspiro.tidal" />
        <package android:name="com.amazon.mp3" />
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_music_note"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_music_note"
        android:supportsRtl="true"
        android:theme="@style/Theme.MusicFinder">

        <!-- Main launcher screen -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Receives shared text from Libby, browsers, PDF readers, etc. -->
        <activity
            android:name=".ShareActivity"
            android:exported="true"
            android:theme="@style/Theme.MusicFinder.Translucent">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
            <!-- Links to shortcuts.xml so MusicFinder appears in Direct Share row -->
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>

        <!-- Camera + OCR entry point -->
        <activity
            android:name=".CameraActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Music picker list (shown when 2-4 matches found) -->
        <activity
            android:name=".PickerActivity"
            android:exported="false"
            android:theme="@style/Theme.MusicFinder.Dialog" />

        <!-- In-app YouTube player — autoplay with sound, no browser needed -->
        <activity
            android:name=".PlayerActivity"
            android:exported="false"
            android:hardwareAccelerated="true" />

    </application>

</manifest>
